#include <stdio.h>
#include <stdlib.h>

#include "legalmoves.h"
#include "../fen/fen.h"

Move* genLegalMoves(Board* board) {
	Move* legalMovesArray = calloc(1, sizeof(Move));

	for (int i = 0;i < 64;i++) {

	}

	return legalMovesArray;
}